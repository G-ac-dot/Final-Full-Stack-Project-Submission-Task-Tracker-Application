# Frontend: Task Tracker App

React app with Tailwind for styling.

## Setup
1. `npm install`
2. Update `api/client.js` baseURL if backend is remote
3. `npm run dev` (runs on http://localhost:5173)

## Components
- Navbar: Auth links
- TaskList: Main view with filters
- TaskForm: Add/edit form
- TaskCard: Individual task display